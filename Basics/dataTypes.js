"use strict"; // treat all JS code as newer version

// alert ( 3 + 3) // We are using nodejs, and not the browser

// We avoid semicolon in nodejs react

let name = "Tejas"
let age = 18
let isLoggedIn = false
let state = null
//Primitive datatypes ------

// number => 2 to power 53 (range)
// bigint (big integer)
// string => " "
// boolean => true /false
// null (it is a datatype along with it is an standalone value)
// undefined => 
// symbol (for finding uniqueness) 

// Object -----

console.log(typeof age)
console.log(typeof undefined)
console.log(typeof null) // Object type
