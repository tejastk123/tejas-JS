const gameName = new String(`This is tejas from COEP`)

console.log(gameName.anchor(4)); // this is like href or html which generates url
console.log(gameName.at(5)); //The at() method of Array instances takes an integer value and returns the item at that index, allowing for positive and negative integers.
console.log(gameName.big(4)); // 
console.log(gameName.blink()); //  In Javascript, the blink() method is a string method that is used to display a string inside the blink tag